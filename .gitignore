venv/
__pycache__/
*.db
*.db-wal
*.db-shm
.env
*.pyc
node_modules/

# Токен Spotify (refresh_token) — приватный, живёт рядом с .env
spotify_token.json

# Внутренняя документация (ТЗ, план) — не публикуем на GitHub
docs/

# Уникальные промпты сервиса — приватно, только локально
prompt_config.py